<?xml version="1.0" encoding="utf-8"?>
<!--
    Premium Doodle navigation host.

    Layout:
       ┌─ slim top bar (LMusic wordmark · settings) ──────────┐
       │  content (Explore / Library child fragments)         │
       │  global mini-player                                  │
       └─ bottom navigation (Library · Search · Favs) ────────┘
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">

    <!-- ── Slim top bar ─────────────────────────────────────────────── -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="20dp"
        android:paddingTop="18dp"
        android:paddingBottom="10dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="LMusic"
            android:textSize="22sp"
            android:textStyle="bold"
            android:letterSpacing="-0.01"
            android:textColor="@color/accent" />

        <ImageButton
            android:id="@+id/btn_global_settings"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_settings"
            android:background="@drawable/bg_circle_glass"
            android:padding="9dp"
            android:contentDescription="Settings" />

    </LinearLayout>

    <!-- ── Content area ─────────────────────────────────────────────── -->
    <!--
        animateLayoutChanges removed: combined with FragmentTransaction
        fade animations it was leaving the visible child at alpha=0 on the
        very first paint (cause of the "Library blank on launch" bug).
    -->
    <FrameLayout
        android:id="@+id/nav_content"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- ── Global mini-player ───────────────────────────────────────── -->
    <include
        android:id="@+id/global_mini_player"
        layout="@layout/layout_mini_player"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone" />

    <!-- ── Bottom navigation (items added programmatically) ─────────── -->
    <LinearLayout
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_bottom_nav"
        android:paddingHorizontal="8dp"
        android:paddingVertical="8dp"
        android:animateLayoutChanges="true" />

</LinearLayout>
